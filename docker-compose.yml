services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
    - ./backend/.env
    volumes:
      - db-data:/data

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  db-data:
